# 數字森林：目前項目狀態

最後更新：2026-08-31

## 狀態摘要

- Repository：`GoldHard1995/digital-forest-math`
- 本機分支：`main`
- 正式網址：<https://digital-forest-math.pages.dev/>
- 前端：`React 19`、`TypeScript`、`Vite`、`Tailwind CSS`
- 後端：`Supabase Auth` 及 `Postgres`
- 部署：`GitHub main` → `Cloudflare Pages`
- 現階段：已建立可登入的低擬真原型；第一關 `13` 題、第二關「比較大小」`13` 題、第三關「同號加法」`13` 題、第四關「異號加法」`13` 題及小頭目「聚能獸」`10` 題（`4／4／2`）均已由教師核准。合併遠端登入修正後的版本（`be17b9b`）已推送至 `GitHub main`，並於 2026-08-31 在 `Cloudflare Pages` 正式網址驗證地圖、第三關及小頭目畫面；正式作答資料及教師報表仍未接駁。

## 最終目標

建立一個供香港中一基礎較弱學生使用的繁體中文數學小遊戲平台。學生透過短關卡及地圖解鎖鞏固有向數，系統保存每次作答、提示、錯誤類型、補救及成長紀錄；教師可安全地管理班級、指派活動、查看個別錯誤及比較平行前後測。

長期階段及驗收門檻見 [`PLAN.md`](PLAN.md)。

## 已完成項目

### 部署及基礎架構

- 已建立 `GitHub` repository 及 `Cloudflare Pages` 正式網址。
- `npm run build` 使用 `vite.static.config.ts`，輸出至 `dist/client`，已通過本機建置。
- 已提供 `.env.example`，前端只讀取 `VITE_SUPABASE_URL` 及 `VITE_SUPABASE_ANON_KEY`。

### 登入及角色分流

- `lib/supabase.ts` 已建立瀏覽器端 `Supabase` client。
- `app/page.tsx` 已接駁密碼登入、登出、session 監聽及 `profiles` 角色資料。
- 學生的非電郵登入名稱會轉換為內部電郵別名；教師仍以電郵登入。
- `student` 角色不顯示教師入口，亦會被畫面守衛帶回學生地圖。
- `teacher`、`admin` 及指定教師帳戶可以進入教師原型畫面。
- `Supabase` 內曾以人工流程建立學生、教師及測試帳戶；其現況、數目及 `RLS` 必須在下一次後端工作開始前重新核對，版本庫沒有可重建的遷移紀錄。

### 第一關「數線方向」

- 已建立 `4` 題基礎回想、`6` 題核心練習及 `3` 題綜合或挑戰，共 `13` 題。
- Q2 已由 `−9` 改為 `−7`。
- 基礎回想及核心練習的數線題使用 `−8` 至 `＋8` 數線。
- 核心練習 C4–C6 為溫度、獲利及方向情境題，按教師指示不顯示數線。
- 已建立平台內數字鍵盤、答案格式統一、答題進度及三次錯誤的逐層回饋畫面。
- 已建立並檢視 `SVG` 數線；另保留 `PNG`、`PDF` 審閱檔及產生腳本。
- 已在 `1024 × 768` 橫屏版面檢查主要答題畫面。

### 第二關「比較大小」（教師已核准，前端試玩已完成）

- 已建立 `4` 題基礎回想（`NF2-A01`–`NF2-A04`，難度 `1`），以 `＜`／`＞` 符號鍵盤作答。
- 已建立 `6` 題核心練習（`NF2-B01`–`NF2-B06`，難度 `2`）；題目統一使用「數字」描述，並以拖拉方式按「由小至大」或「由大至小」排列。
- 已建立 `3` 題綜合或挑戰（`NF2-H01`–`NF2-H03`，難度 `3`）；按教師核准加入正、負小數，並與 B 部分一樣以拖拉排列作答。
- 題目及答案集中於 [`lib/second-stage-content.ts`](lib/second-stage-content.ts)，由 [`app/page.tsx`](app/page.tsx) 的第二關畫面讀取；第一關內容檔未有改動。
- 第二關只在示範模式開放，尚未保存作答、星級；已隨本版本推送並部署至正式網址。

### 第三關「同號加法」（教師已核准，前端試玩已完成）

- 已建立 `4` 題基礎回想（`NF3-A01`–`NF3-A04`，難度 `1`），以數字鍵盤輸入整數答案。
- 已建立 `6` 題核心練習（`NF3-B01`–`NF3-B06`，難度 `2`），以數字鍵盤輸入整數答案，題目均為同號有向數相加。
- 已建立 `3` 題綜合或挑戰（`NF3-H01`–`NF3-H03`，難度 `3`）；H01 為負小數情境，H02 已按教師修訂為正數小數題，H03 為正數小數情境；每個小數最多 1 位。
- 題目及答案集中於 [`lib/third-stage-content.ts`](lib/third-stage-content.ts)，由 [`app/page.tsx`](app/page.tsx) 的第三關畫面讀取；第三關地圖節點已開放試玩。
- 第三關只在示範模式開放，尚未保存作答、星級；已隨本版本推送並部署至正式網址。

### 第四關「異號加法」（教師已核准，前端試玩已完成）

- 已建立 `4` 題基礎回想（`NF4-A01`–`NF4-A04`，難度 `1`）；每題為兩個異號有向數相加，數字均為個位數。
- 已建立 `6` 題核心練習（`NF4-B01`–`NF4-B06`，難度 `2`）；每題只含兩個異號有向數，兩個數字均為兩位數。
- 已建立 `3` 題綜合或挑戰（`NF4-C01`–`NF4-C03`，難度 `3`）；每題為三個有向數相加，維持整數運算。
- 題目及答案集中於 [`lib/fourth-stage-content.ts`](lib/fourth-stage-content.ts)，由 [`app/page.tsx`](app/page.tsx) 的第四關畫面讀取；第四關地圖節點已開放試玩。
- 第四關只在示範模式開放，尚未保存作答、星級；已隨本版本推送並部署至正式網址。

### 小頭目「聚能獸」（教師已核准，前端試玩已完成）

- 已建立 `10` 題加法挑戰，分為 A 部分 `4` 題基礎回想、B 部分 `4` 題核心練習及 C 部分 `2` 題綜合或挑戰；題目編號為 `NF5-A01`–`NF5-C02`，難度依次為 `1／2／3`。
- 小頭目整合前四關的同號及異號有向數加法，全部使用整數數字鍵盤；答錯時沿用逐層提示及完整解法。
- 聚能獸設有 `10` 格血量；每答對一題減少 `1` 格，完成第 `10` 題後顯示擊退結果並可返回地圖。
- 題目及答案集中於 [`lib/boss-stage-content.ts`](lib/boss-stage-content.ts)，由 [`app/page.tsx`](app/page.tsx) 的小頭目畫面讀取；地圖節點已開放試玩。
- 小頭目只在示範模式開放，尚未保存作答、血量、星級；已隨本版本推送並部署至正式網址。

## 目前未提交改動

目前工作樹沒有未提交改動；`main` 與 `origin/main` 已同步。小頭目、第四關、第三關、第二關及第一關的已核准內容，連同遠端登入修正，已由合併提交 `be17b9b` 推送至 `GitHub`，並已在正式網址完成基本頁面驗證。

## 尚未完成項目

### 第一關

- `lib/first-stage-content.ts` 已有 `NF-H01`–`NF-H03` 共 `3` 題只考方向、位置及距離的綜合或挑戰題，已獲教師核准為難度 `3`。
- 內容 QA 已完成：題目編號 `13` 個且不重複，分段為 `4 / 6 / 3`，每題均有答案、顯示答案及解法。
- 現有提示流程尚未在第三次錯誤後自動產生同技能、不同數值的補救題。
- 完成 `13` 題後只返回地圖，尚未保存星級、掌握或更新地圖進度。
- 作答、首次正確、提示、完整解法及錯誤類型尚未寫入 `Supabase`。

### 內容及遊戲

- 第一區其餘四項技能尚未建立正式題目及學習流程。
- 大頭目、收藏、徽章、完整內容解鎖及能力軌跡尚未完成。
- 題目版本、發布、封存及歷史作答保留尚未實作。

### 後端及教師功能

- 資料庫 schema、migration、seed 及 `RLS` 測試未納入 repository，現時不能由程式碼重建後端。
- 教師總覽、學生報表、指派活動及匯出按鈕仍使用 `app/page.tsx` 的硬編碼示範資料。
- 班級建立、`Excel` 匯入、登入資料產生、重設密碼及封存流程尚未接駁。
- 前後測、重玩紀錄、錯誤診斷信心及正式成效報告尚未完成。
- 單一有效作答工作階段、斷線同步及重複提交保護尚未完成。

### 營運及品質

- 尚未完成 `70` 人同時在線測試、`iPadOS 15 Safari` 全流程測試及無障礙檢查。
- 備份、容量警告、錯誤監察、維護畫面及資料刪除流程尚未完成。
- `README.md` 仍表示 `Supabase` 未連接，已落後於實際程式碼，需要稍後修正。
- 2026-08-31 重新驗證：第二關內容 `13` 題、分段 `4 / 6 / 3`、題目編號不重複；本地靜態預覽已實際完成地圖 → 第二關、A 符號判分、B 拖拉判分及 H 正負小數拖拉流程；`npm run lint`、`npm run build` 及 `git diff --check` 均通過。拖拉排列仍需在實體 `iPadOS 15 Safari` 完整驗證。
- 2026-08-31 重新驗證：第三關內容 `13` 題、分段 `4 / 6 / 3`、題目編號不重複；本地靜態預覽已實際完成地圖 → 第三關、A/B 整數鍵盤判分、H 負小數判分、H02 正小數題及 H03 正小數題；H 輸入超過 1 位小數會限制為 1 位。`npm run lint`、`npm run build` 及 `git diff --check` 均通過。第三關仍需在實體 `iPadOS 15 Safari` 驗證。
- 2026-08-31 重新驗證：第四關內容 `13` 題、分段 `4 / 6 / 3`、題目編號不重複；本地靜態預覽已實際完成地圖 → 第四關、A 個位數判分、B 兩位數雙項判分及 C 三項有向數判分；第四關鍵盤不顯示小數鍵。`npm run lint`、`npm run build` 及 `git diff --check` 均通過。第四關仍需在實體 `iPadOS 15 Safari` 驗證。
- 2026-08-31 重新驗證：小頭目內容 `10` 題、分段 `4 / 4 / 2`、題目編號不重複；本地靜態預覽已實際完成地圖 → 聚能獸、整數數字鍵盤判分、答對後血量逐格由 `10` 減至 `0`，並顯示完成挑戰按鈕。`npm run lint`、`npm run build` 及 `git diff --check` 均通過；小頭目仍需在實體 `iPadOS 15 Safari` 驗證。
- 2026-08-31 正式部署驗證：`GitHub main` 已推送至 `Cloudflare Pages`；公開頁面可載入學生地圖，並可進入第三關（顯示 `13` 題）及小頭目（顯示 `10／10` 格血量）。實體 `iPadOS 15 Safari` 及正式作答資料仍需另行驗證。

## 目前架構及重要檔案

- [`app/page.tsx`](app/page.tsx)：目前大部分登入、學生地圖、答題及教師原型畫面；仍是單檔低擬真結構。
- [`lib/first-stage-content.ts`](lib/first-stage-content.ts)：第一關已核准的 `13` 題內容及數線設定。
- [`lib/second-stage-content.ts`](lib/second-stage-content.ts)：第二關已核准的 `13` 題比較大小內容及互動設定。
- [`lib/third-stage-content.ts`](lib/third-stage-content.ts)：第三關已核准的 `13` 題同號加法內容；H 部分每個小數最多 1 位。
- [`lib/fourth-stage-content.ts`](lib/fourth-stage-content.ts)：第四關已核准的 `13` 題異號加法內容；A 為個位數，B 為兩位數雙項相加，C 為三項相加。
- [`lib/boss-stage-content.ts`](lib/boss-stage-content.ts)：小頭目已核准的 `10` 題有向數加法內容；分段為 `4／4／2`，使用整數數字鍵盤及逐格血量。
- [`lib/supabase.ts`](lib/supabase.ts)：瀏覽器端 `Supabase` client。
- [`vite.static.config.ts`](vite.static.config.ts)：`Cloudflare Pages` 正式靜態建置設定。
- [`vite.config.ts`](vite.config.ts)：`Vinext`／Sites 設定，不是目前 `Cloudflare Pages` 靜態建置入口。
- [`scripts/draw_number_line.py`](scripts/draw_number_line.py)：重建數線審閱及網站資源。
- [`docs/architecture.md`](docs/architecture.md)：架構、資料流、環境變數及建置方式。
- [`docs/decisions.md`](docs/decisions.md)：已作出的技術及教學實作決定。
- [`docs/requirements.md`](docs/requirements.md)：目前產品要求及驗收條件。

## 已作出的技術決定及原因

- 使用 `Cloudflare Pages` 靜態建置：符合免費試用及目前前端需求，亦避開 `Vinext` 設定依賴 `.openai/hosting.json` 導致的建置問題。
- 使用 `Supabase`：以同一免費後端提供身份驗證、`Postgres` 及 `RLS`。
- 學生登入名稱在前端轉為內部電郵別名：保留學生使用數字帳號的體驗，同時沿用 `Supabase` 電郵密碼驗證。
- 題目先放在本機內容檔：第一版需教師逐題核實，核准及版本化流程未完成前不直接把生成題發布至正式題庫。
- 數線以 `SVG` 顯示：在舊 `iPad` 保持清晰並減少點陣圖放大失真。

完整決定及限制見 [`docs/decisions.md`](docs/decisions.md)。

## 已知問題／Bug

- 教師畫面的學生、進度、正確率及錯誤類型是示範資料，不代表 `Supabase` 現況。
- `demoMode` 會顯示教師畫面，只適合本機原型；正式部署前應評估關閉或只在開發環境開啟。
- 學生畫面的進度、星級及掌握狀態未保存，重新登入後不能延續。
- 第三次錯誤後尚未出現正式補救題，與產品需求不完整一致。
- 前端角色守衛不是資料安全邊界；若 `RLS` 不完整，仍有資料洩漏風險。
- repository 沒有後端 migration，因此人工建立的資料表、帳戶及政策可能與程式碼漂移。
- `README.md` 的後端狀態已過時。
- 本機 `vinext dev` 在受限制執行環境可能因 inspector port 權限失敗；靜態 `Vite` 建置及預覽可用。

## 下一步應該做甚麼

1. 在有實體裝置時完成第二關拖拉、第三關、第四關及小頭目數字鍵盤於 `iPadOS 15 Safari` 的橫屏驗證，並記錄結果。
2. 依教師要求先建立第一區其餘四項技能；每項題目先列出供核准，不發布未核准題目。
3. 已完成本輪已核准關卡的整批推送及 `Cloudflare Pages` 正式網址核對；日後新增題目仍須教師核准後才可加入下一次發布。
4. 把 `Supabase` schema、migration、seed 及 `RLS` 政策納入 repository；先以無個人資料的測試資料驗證。
5. 以正式資料保存題目版本、活動、作答、提示、補救、星級及進度，移除相關硬編碼狀態。
6. 使用學生測試帳戶完成角色隔離及完整答題流程測試，再開始教師報表接駁。
7. 每完成一個重要階段，立即更新本文件的日期、已完成、未完成、問題及下一步。

## 哪些地方不要改動

- 不要重設、丟棄或覆蓋上述未提交改動；先檢查 `git status` 及實際 diff。
- 不要把真實學生資料、教師資料、密碼、匯入檔案、service role key 或其他秘密提交至 `Git` 或寫入本文件。
- 不要在瀏覽器端使用 `Supabase service_role`；資料權限必須由 `RLS` 保護。
- 不要把教師原型的硬編碼數據描述為正式報告。
- 不要改回會讓學生看見教師畫面的角色邏輯。
- 不要修改已核准的第一關 `13` 題、Q2 的 `−7`、C4–C6 不設數線等教學決定，除非教師明確要求。
- 除教師已核准的第二關 `NF2-H01`–`NF2-H03` 正、負小數及第三關 `NF3-H01`–`NF3-H03` 每個小數最多 1 位外，首版不要加入分數、其他小數、未知數填空、自由換裝、音效、語音或朗讀。
- 不要把 `Cloudflare Pages` 正式建置改為 `vite.config.ts`；目前應使用 `npm run build` 及輸出 `dist/client`。
- 不要直接覆寫已產生作答紀錄的題目；使用新版本並保留歷史。
- 不要因文件描述而忽略程式碼；文件與 repository 不一致時，以可重現的實際狀態為準並修正文件。

## 新工作階段的操作規則

開始前閱讀 `PLAN.md` 及本文件，再檢查 `git status`、近期提交、環境設定及實際程式碼。不要重新製作已完成的功能。每完成一個重要階段後更新本文件，而 `PLAN.md` 只在長期方向改變時更新。
